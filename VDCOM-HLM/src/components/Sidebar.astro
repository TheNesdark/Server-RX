<aside>
    <h2>IPS Hospital Local de Montelíbano</h2>
    <nav class="nav-menu">
        <ul>
            <li>
                <a href="index.html" class="active"
                    ><span class="icon">🔍</span> Búsqueda Estudios</a
                >
            </li>
            <li>
                <label
                    for="dicom-file-input"
                    style="cursor:pointer; display:flex; align-items:center;"
                >
                    <span class="icon">👥</span> Cargar
                </label>
                <input
                    id="dicom-file-input"
                    type="file"
                    accept=".dcm,application/dicom"
                    style="display:none"
                />
            </li>
            <li>
                <a href="#" data-page="settings"
                    ><span class="icon">⚙️</span> Configuración</a
                >
            </li>
        </ul>
    </nav>
    <div class="sidebar-footer">
        <a href="#" class="logout">Cerrar Sesión</a>
    </div>
</aside>

<style>
    :root {
        --bg-dark: #0f1115;
        --bg-panel: #181b21;
        --bg-hover: #232730;
        --accent-color: #3b82f6;
        --text-main: #e2e8f0;
        --text-muted: #94a3b8;
        --border-color: #2d3748;
    }

    aside {
        width: 280px;
        background: var(--bg-panel);
        padding: 0;
        box-shadow: 4px 0 20px rgba(0, 0, 0, 0.2);
        display: flex;
        flex-direction: column;
        border-right: 1px solid var(--border-color);
        overflow: hidden;
    }

    aside h2 {
        padding: 24px 24px 16px;
        color: var(--text-main);
        font-size: 18px;
        font-weight: 600;
        text-align: left;
        border-bottom: 1px solid var(--border-color);
        margin: 0;
        background: var(--bg-panel);
    }

    .nav-menu {
        flex: 1;
        padding: 12px 0;
    }

    aside ul {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    aside li {
        margin: 4px 12px;
    }

    aside a,
    aside label {
        display: flex;
        align-items: center;
        padding: 12px 16px;
        color: var(--text-muted);
        text-decoration: none;
        font-size: 14px;
        font-weight: 500;
        transition: all 0.2s ease;
        border-radius: 8px;
        cursor: pointer;
    }

    aside a:hover,
    aside label:hover {
        background: var(--bg-hover);
        color: var(--text-main);
    }

    aside a.active {
        background: rgba(59, 130, 246, 0.15);
        color: var(--accent-color);
    }

    aside .icon {
        margin-right: 12px;
        font-size: 18px;
        opacity: 0.8;
    }

    .sidebar-footer {
        padding: 16px 24px;
        border-top: 1px solid var(--border-color);
        background: var(--bg-panel);
    }

    .logout {
        display: block;
        color: #ef4444;
        text-decoration: none;
        font-size: 14px;
        padding: 8px 0;
        transition: color 0.2s;
    }

    .logout:hover {
        color: #f87171;
        text-decoration: none;
    }
</style>
