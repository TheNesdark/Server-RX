---
import { HelpIcon, SearchIcon, EditIcon, SunIcon } from "@/components/icons";
---

<!-- Modal de Ayuda -->
<div id="help-modal" class="modal">
    <div class="modal-content">
        <div class="modal-header">
            <div class="header-title">
                <HelpIcon size={24} stroke="#3b82f6" />
                <h2>Guia de Herramientas RX</h2>
            </div>
            <button id="modal-close" class="modal-close" aria-label="Cerrar"
                >&times;</button
            >
        </div>
        <div class="modal-body">
            <div class="help-section">
                <div class="section-header">
                    <SearchIcon size={20} />
                    <h3>Visualización</h3>
                </div>
                <div class="help-grid">
                    <div class="help-item">
                        <strong>Ventana/Nivel RX</strong>
                        <p>
                            Ajusta nivel de gris y contraste para resaltar
                            estructuras oseas y tejidos blandos.
                        </p>
                    </div>
                    <div class="help-item">
                        <strong>Zoom RX + Pan</strong>
                        <p>
                            Usa la rueda del raton para acercar/alejar y
                            arrastra para mover la placa.
                        </p>
                    </div>
                    <div class="help-item">
                        <strong>Serie de placas</strong>
                        <p>
                            Si el estudio incluye varias proyecciones, cambia
                            de serie desde el panel lateral.
                        </p>
                    </div>
                </div>
            </div>

            <div class="help-section">
                <div class="section-header">
                    <EditIcon size={20} />
                    <h3>Anotaciones</h3>
                </div>
                <div class="help-item draw-info">
                    <strong>Herramientas de Dibujo</strong>
                    <p>
                        Usa el menú desplegable para añadir marcas precisas al
                        estudio:
                    </p>
                    <ul class="tool-list">
                        <li><span>Arrow</span> Indica hallazgos</li>
                        <li><span>Ruler</span> Mide distancias (mm)</li>
                        <li>
                            <span>Circle/Ellipse</span> Encierra áreas de interés
                        </li>
                        <li><span>Rectangle</span> Delimita regiones</li>
                        <li><span>Protractor</span> Mide ángulos exactos</li>
                    </ul>
                </div>
            </div>

            <div class="help-section">
                <div class="section-header">
                    <SunIcon size={20} />
                    <h3>Funciones Avanzadas</h3>
                    <div class="help-grid">
                        <div class="help-item">
                            <strong>Orientation Markers</strong>
                            <p>
                                Muestra referencias anatomicas de orientacion
                                para una lectura mas segura del estudio.
                            </p>
                        </div>
                        <div class="help-item">
                            <strong>Scale Overlay</strong>
                            <p>
                                Activa una regla visual de escala para apoyar
                                la estimacion de tamano en pantalla.
                            </p>
                        </div>
                        <div class="help-item highlight">
                            <strong>Reset</strong>
                            <p>
                                Vuelve al estado inicial de la imagen con un
                                solo clic.
                            </p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="help-footer-tip">
                <div class="tip-icon">💡</div>
                <p>
                    Haz clic nuevamente en cualquier herramienta activa para
                    desactivarla.
                </p>
            </div>
        </div>
    </div>
</div>

<script>
    const HelpModal = document.getElementById("help-modal");
    const modalClose = document.getElementById("modal-close");
    const modalOpen = document.getElementById("modal-open");

    modalOpen?.addEventListener("click", () => {
        HelpModal?.classList.add("show");
    });

    modalClose?.addEventListener("click", () => {
        HelpModal?.classList.remove("show");
    });

    window.addEventListener("click", (event) => {
        if (event.target === HelpModal) {
            HelpModal?.classList.remove("show");
        }
    });

    // Cerrar con tecla Escape
    window.addEventListener("keydown", (e) => {
        if (e.key === "Escape" && HelpModal?.classList.contains("show")) {
            HelpModal?.classList.remove("show");
        }
    });
</script>

<style>
    /* Modal Base */
    .modal {
        display: none;
        position: fixed;
        inset: 0;
        z-index: 2000;
        background-color: rgba(0, 0, 0, 0.85);
        backdrop-filter: blur(8px);
        animation: fadeIn 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .modal.show {
        display: flex;
        align-items: center;
        justify-content: center;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }

    /* Modal Content Container */
    .modal-content {
        background: #111;
        background-image: radial-gradient(circle at top left, #1a1a1a, #0a0a0a);
        border-radius: 16px;
        max-width: 640px;
        width: 90%;
        max-height: 85vh;
        overflow: hidden;
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.7);
        border: 1px solid #333;
        display: flex;
        flex-direction: column;
        animation: scaleUp 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    }

    @keyframes scaleUp {
        from {
            transform: scale(0.95) translateY(20px);
            opacity: 0;
        }
        to {
            transform: scale(1) translateY(0);
            opacity: 1;
        }
    }

    /* Header Styling */
    .modal-header {
        padding: 24px;
        background: rgba(255, 255, 255, 0.03);
        border-bottom: 1px solid #222;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .header-title {
        display: flex;
        align-items: center;
        gap: 12px;
    }

    .header-title h2 {
        margin: 0;
        color: #fff;
        font-size: 20px;
        font-weight: 600;
        letter-spacing: -0.5px;
    }

    .modal-close {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid #333;
        color: #888;
        font-size: 24px;
        cursor: pointer;
        width: 36px;
        height: 36px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.2s ease;
    }

    .modal-close:hover {
        background: #ef4444;
        color: white;
        border-color: #ef4444;
        transform: rotate(90deg);
    }

    /* Body and Grid */
    .modal-body {
        padding: 24px;
        overflow-y: auto;
    }

    .help-section {
        margin-bottom: 32px;
    }

    .section-header {
        display: flex;
        align-items: center;
        gap: 10px;
        color: #3b82f6;
        margin-bottom: 16px;
    }

    .section-header h3 {
        margin: 0;
        font-size: 16px;
        text-transform: uppercase;
        letter-spacing: 1px;
        font-weight: 700;
    }

    .help-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 16px;
    }

    .help-item {
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid #222;
        padding: 16px;
        border-radius: 12px;
        transition: all 0.2s ease;
    }

    .help-item:hover {
        background: rgba(255, 255, 255, 0.05);
        border-color: #444;
        transform: translateY(-2px);
    }

    .help-item strong {
        color: #fff;
        display: block;
        margin-bottom: 6px;
        font-size: 14px;
        font-weight: 600;
    }

    .help-item p {
        color: #999;
        margin: 0;
        font-size: 13px;
        line-height: 1.5;
    }

    /* Special sections */
    .draw-info {
        border-left: 3px solid #3b82f6;
    }

    .tool-list {
        list-style: none;
        padding: 0;
        margin: 12px 0 0 0;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 8px;
    }

    .tool-list li {
        font-size: 12px;
        color: #bbb;
        display: flex;
        flex-direction: column;
    }

    .tool-list span {
        font-weight: 600;
        color: #3b82f6;
    }

    .highlight {
        background: rgba(16, 185, 129, 0.05);
        border-color: rgba(16, 185, 129, 0.2);
    }

    /* Footer Tip */
    .help-footer-tip {
        display: flex;
        align-items: center;
        gap: 12px;
        background: rgba(59, 130, 246, 0.1);
        border: 1px dashed rgba(59, 130, 246, 0.3);
        padding: 12px 16px;
        border-radius: 12px;
        margin-top: 8px;
    }

    .tip-icon {
        font-size: 20px;
    }

    .help-footer-tip p {
        margin: 0;
        color: #3b82f6;
        font-size: 13px;
        font-weight: 500;
    }
</style>
